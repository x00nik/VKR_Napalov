<Window x:Class="DeepfakeDetectorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Детектор Дипфейков Аудио" 
        Height="750" Width="520"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и статус сервера -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🎙️ Детектор Дипфейков Аудио" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#212121"/>
            <TextBlock x:Name="ServerStatusText" 
                       Text="Проверка подключения..." 
                       FontSize="13" 
                       Margin="0,8,0,0"
                       Foreground="#757575"/>
            
            <!-- Настройка URL сервера -->
            <Border Background="White" 
                    CornerRadius="6" 
                    Padding="12" 
                    Margin="0,12,0,0"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="🌐" 
                               Grid.Column="0"
                               FontSize="16" 
                               VerticalAlignment="Center" 
                               Margin="0,0,8,0"/>
                    
                    <TextBox x:Name="ServerUrlBox" 
                             Grid.Column="1"
                             Text="http://localhost:5000" 
                             FontSize="13" 
                             VerticalAlignment="Center"
                             Padding="8,6"
                             BorderBrush="#E0E0E0"
                             BorderThickness="1"/>
                    
                    <Button x:Name="UpdateServerBtn" 
                            Grid.Column="2"
                            Content="✓" 
                            Width="32" 
                            Height="32" 
                            FontSize="16"
                            Margin="8,0,0,0"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="UpdateServerBtn_Click"
                            ToolTip="Применить новый URL сервера"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Основной контент -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Выбор устройства вывода -->
                <Border Background="White" 
                        CornerRadius="8" 
                        Padding="20" 
                        Margin="0,0,0,15"
                        Effect="{DynamicResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="Устройство вывода (для записи)" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="#212121" 
                                   Margin="0,0,0,12"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <ComboBox x:Name="MicrophoneCombo" 
                                      Grid.Column="0"
                                      Height="40" 
                                      FontSize="14" 
                                      Margin="0,0,10,0"/>
                            
                            <Button x:Name="RefreshBtn" 
                                    Grid.Column="1"
                                    Content="🔄" 
                                    Width="40" 
                                    Height="40" 
                                    FontSize="18"
                                    Click="RefreshBtn_Click"
                                    Cursor="Hand"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Настройки записи -->
                <Border Background="White" 
                        CornerRadius="8" 
                        Padding="20" 
                        Margin="0,0,0,15"
                        Effect="{DynamicResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="Длительность полной записи (сек)" 
                                   FontSize="14" 
                                   Foreground="#757575" 
                                   Margin="0,0,0,8"/>
                        <TextBox x:Name="DurationBox" 
                                 Text="10" 
                                 Height="40" 
                                 FontSize="16" 
                                 VerticalContentAlignment="Center" 
                                 Padding="12,0"/>
                        <TextBlock Text="ℹ️ Сначала записывается 5 сек для быстрой проверки" 
                                   FontSize="12" 
                                   Foreground="#757575" 
                                   Margin="0,8,0,0" 
                                   TextWrapping="Wrap"
                                   FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <!-- Статус записи -->
                <Border Background="White" 
                        CornerRadius="8" 
                        Padding="20" 
                        Margin="0,0,0,15"
                        Effect="{DynamicResource CardShadow}">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border x:Name="StatusIndicator" 
                                    Grid.Column="0"
                                    Width="12" 
                                    Height="12" 
                                    CornerRadius="6" 
                                    Background="#9E9E9E" 
                                    Margin="0,0,12,0" 
                                    VerticalAlignment="Center"/>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock x:Name="StatusText" 
                                           Text="Готов к записи" 
                                           FontSize="15" 
                                           FontWeight="SemiBold" 
                                           Foreground="#212121"/>
                                <TextBlock x:Name="SubStatusText" 
                                           Text="Нажмите кнопку для начала" 
                                           FontSize="13" 
                                           Foreground="#757575" 
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>

                        <ProgressBar x:Name="ProgressBar" 
                                     Height="8" 
                                     Margin="0,0,0,15" 
                                     Maximum="100" 
                                     Value="0"/>

                        <Button x:Name="RecordBtn" 
                                Content="🎙️ НАЧАТЬ ЗАПИСЬ" 
                                Height="50" 
                                FontSize="16" 
                                FontWeight="Bold" 
                                Foreground="White" 
                                Background="#2196F3"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="RecordBtn_Click"/>
                    </StackPanel>
                </Border>

                <!-- Результаты анализа -->
                <Border x:Name="ResultCard"
                        Background="White" 
                        CornerRadius="8" 
                        Padding="20" 
                        Visibility="Collapsed"
                        Effect="{DynamicResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="Результаты анализа" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="#212121" 
                                   Margin="0,0,0,15"/>
                        
                        <Border x:Name="ResultIndicator"
                                Background="#4CAF50" 
                                CornerRadius="8" 
                                Padding="20" 
                                Margin="0,0,0,15">
                            <TextBlock x:Name="ResultText" 
                                       Text="✓ НАСТОЯЩИЙ ГОЛОС" 
                                       FontSize="20" 
                                       FontWeight="Bold" 
                                       Foreground="White" 
                                       TextAlignment="Center"/>
                        </Border>

                        <TextBlock x:Name="ConfidenceText" 
                                   Text="Уверенность: 95%" 
                                   FontSize="15" 
                                   Foreground="#212121" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,0,0,20"/>

                        <TextBlock Text="Детали по моделям:" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="#212121" 
                                   Margin="0,0,0,10"/>

                        <StackPanel x:Name="ModelDetailsPanel">
                            <!-- Модельные результаты будут добавлены динамически -->
                        </StackPanel>

                        <Separator Margin="0,15,0,15" 
                                   Background="#E0E0E0" 
                                   Height="1"/>

                        <StackPanel Orientation="Horizontal" 
                                    HorizontalAlignment="Center">
                            <TextBlock Text="⏱️" 
                                       FontSize="14" 
                                       Margin="0,0,8,0"/>
                            <TextBlock x:Name="ProcessingTimeText" 
                                       Text="Время обработки: 1.25с" 
                                       FontSize="13" 
                                       Foreground="#757575"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>

    <!-- Ресурсы для теней -->
    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow" 
                          BlurRadius="10" 
                          ShadowDepth="2" 
                          Opacity="0.15" 
                          Color="#000000"/>
    </Window.Resources>
</Window>

